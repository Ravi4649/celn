# ── CELN v3 Core Dependencies ──────────────────────────────────
# Required for the reasoning engine and text generation pipeline.

numpy>=1.21.0             # Core vector operations, FFT binding
numba>=0.56.0             # JIT-compiled inner loops (@njit)

# ── Optional ────────────────────────────────────────────────────
# spacy>=3.5.0           # Morphological inflection in linearizer.py (lazy import)
# scikit-learn           # PCA / SVD baselines (experiments only)
# psutil                 # Memory profiling (stress tests only)
# datasets               # Load ProofWriter dataset for benchmarks
